# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Firebase Hosting Shopping Card
'on':
  push:
    branches:
      - main

    paths:
      - projects/app-project-shopping-card/**
      - .github/workflows/firebase-hosting-shop-merge.yml
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_GALAXY_TRAINING }}'
          channelId: live
          projectId: galaxy-training
          target: shopping-card-app
