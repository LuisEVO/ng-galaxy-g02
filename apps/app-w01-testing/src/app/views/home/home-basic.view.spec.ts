/*
import { HomeView } from "./home.view"

describe('HomeView', () => {
  let instance: HomeView;

  beforeAll(() => {})
  afterAll(() => {})
  beforeEach(() => {
    instance = new HomeView();
  })
  afterEach(() => {})

  it('Se debe crear la instancia de HomeView', () => {
    expect(instance).toBeDefined();

  })

  it('Se debe agregar una tarea', () => {
    instance.title = 'testing';

    instance.add()

    expect(instance.tasks.length).toBe(1);
    expect(instance.title).toBe('');
  })

  it('Se debe eliminar una tarea', () => {
    instance.title = 'testing';

    instance.add()
    instance.remove(0)

    expect(instance.tasks.length).toBe(0);
  })

})
*/