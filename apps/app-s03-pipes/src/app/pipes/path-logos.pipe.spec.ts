import { PathLogosPipe } from './path-logos.pipe';

describe('PathLogosPipe', () => {
  it('create an instance', () => {
    const pipe = new PathLogosPipe();
    expect(pipe).toBeTruthy();
  });
});
